import { PainelFiguresPage } from "@/components/painel/painel-figures-page"
import { PAINEL_CNIR_CONFIG } from "@/config/painel-cnir"

export function PainelCnirPage() {
  return <PainelFiguresPage config={PAINEL_CNIR_CONFIG} />
}
